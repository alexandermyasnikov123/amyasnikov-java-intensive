package net.dunice.intensive.spring_boot.controllers;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(value = "/quizzes")
public class QuizzesController {
/*
    @GetMapping
    public ResponseEntity<?> listAll() {
        // TODO
    }

    @GetMapping(value = "/{id}")
    public ResponseEntity<?> getById(@PathVariable Long id) {
        //TODO
    }

    @PatchMapping(value = "/{id}/title")
    public ResponseEntity<?> updateTitle(@PathVariable Long id, @RequestBody String title) {
        //TODO
    }

    @PatchMapping(value = "/{id}/add-answer")
    public ResponseEntity<?> addAnswer(@PathVariable Long id, @RequestBody String answer) {
        //TODO
    }

    @PatchMapping(value = "/{id}/remove-answer")
    public ResponseEntity<?> removeAnswer(@PathVariable Long id, @RequestBody String answer) {
        //TODO
    }

    @PostMapping
    public ResponseEntity<?> createQuiz(@RequestBody QuizDto dto, List<MultipartFile> images) {
        //TODO
    }

    @DeleteMapping(value = "/{id}")
    public ResponseEntity<?> deleteQuiz(@PathVariable Long id) {
        //TODO
    }*/
}
